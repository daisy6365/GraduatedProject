<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="message"
            type="com.example.graduatedproject.model.Message" />

    </data>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingVertical="10dp"
        android:paddingLeft="15dp"
        android:paddingRight="60dp"
        android:clipToPadding="false">

        <!--<ImageView
            android:id="@+id/other_image"
            android:layout_alignParentLeft="true"
            android:scaleType="centerInside"
            android:src="@drawable/cat"
            android:alpha="0.8"
            android:layout_width="50dp"
            android:layout_height="50dp" />-->

        <TextView
            android:id="@+id/other_name"
            android:layout_marginLeft="15dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="4dp"
            android:textColor="@color/colorBlack"
            android:textSize="20sp"
            android:text="@{message.sender}" />

        <TextView
            android:id="@+id/other_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/other_name"
            android:layout_alignLeft="@+id/other_name"
            android:background="@drawable/chat_you_style"
            android:elevation="2dp"
            android:paddingHorizontal="16dp"
            android:paddingVertical="12dp"
            android:text="@{message.message}"
            android:textColor="@color/colorBlack"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/other_time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/other_name"
            android:layout_marginStart="30dp"
            android:layout_marginLeft="30dp"
            android:layout_marginTop="20dp"
            android:layout_toRightOf="@+id/other_message"
            android:padding="5dp"
            android:text="@{message.createdAt}"
            android:textColor="@color/colorBlack"
            android:textSize="10sp" />

    </RelativeLayout>
</layout>